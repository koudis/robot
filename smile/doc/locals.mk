# -------------------------------------------------
# LOCALS
# variables for make and paths to executables
#
# add path to executable if program is stored in some nonstandard place or add namo if program is instaled with apt or make install
# and executable is in search path
# -------------------------------------------------

INKSCAPE=inkscape
PDFLATEX=pdflatex
EAGLE=